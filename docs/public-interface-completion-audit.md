# Public interface completion audit

This audit maps the approved implementation plan to current owners and executable
contracts. It accompanies the milestone progress log; it does not replace the
independent architecture reviews. All milestones 0–8 have completed their
implementation and review/fix cycles. The final [milestone 8 review](milestone-8-architecture-review.md)
records its findings, remedies and validation.

| Requirement                                | Current owner and evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Baseline and migration constraints         | `artifacts/public-interface-m0/` preserves production and retention baselines. `scripts/check-editor-performance.mjs` retains the original budgets. Root `pnpm check` still requires the existing lint, formatter, type, declaration, ownership and test checks.                                                                                                                                                                                                               |
| Real headless seams                        | `model` owns definitions/content; `transform` owns operations/mapping; `state` owns publication, selections, history and retained references; `core` composes extensions and commands. Syntax-based dependency checks include type imports, re-exports and dynamic imports. `tests/consumers/headless.mjs` runs built JavaScript in ordinary Node and checks runtime class identity.                                                                                           |
| Typed Standard Schema assembly             | `packages/model/src/definitions.ts` and `assembly.ts` derive content from installed definitions. Model assembly/attribute-normalization tests cover nested issues, canonical ownership, versions, constraints and synchronous rejection of async validation. `tests/schema-assembly.test.ts` covers custom fields, grouped children, edits, codecs and no full-document validation while typing. Compile-time negative cases are part of the required type/declaration checks. |
| Durable positions separate from history    | Model stores reference values, transform maps operations, and state retains resolution data. `tests/durable-document-ranges.test.js` exercises unregistered ranges through nested edits, deletion, undo and checkpoint reload. Core history tests cover independent stack retention and rejected replay.                                                                                                                                                                       |
| Session and imperative command composition | Core session/capability tests cover inferred names, collisions, fresh per-session state, draft chains, rollback, live availability/activity and events. State publication tests and core view-effect tests cover reentrancy, ordering, pure dry runs and effects tied to one view attachment.                                                                                                                                                                                  |
| Complete framework-independent mount       | `@gprose/view` exposes `mountEditor` with readiness, assets, configuration, geometry and destruction. The canvas implementation owns caches, input and painting. Built vanilla consumers edit without engine factories; mounted tests cover failures, cancellation, replacement, two owners and native controls. React delegates to the same mount.                                                                                                                            |
| Configurable presentation                  | View themes and font catalogs compile per-view styles and resolved faces. Standard document/table extensions provide optional defaults. Typography and font browser tests cover separate themes, font replacement, paint-only invalidation, native cell styles, custom rules and anchored live reflow.                                                                                                                                                                         |
| Extension renderers and React              | Public node/mark/inline/widget registrations and content slots own geometry and lifecycle. React portal tests cover context, wrapping, scoped selection, access changes, culling and interactive controls. Ownership tests cover Strict Mode, abandoned renders, hydration, borrowed sessions and selective subscriptions. Comments, search and mentions use these contracts.                                                                                                  |
| Codecs and native input                    | Model/core serializers and view HTML parsers compose extension contributions. Document, table and editing packages own standard rules. Codec, parser, shortcut, input-rule and clipboard tests cover rich rectangular table content, marks, inline values, invalid inputs, precedence, IME and atomic undo.                                                                                                                                                                    |
| Delayed integration edits                  | `createPendingEdit` captures a durable target, supports cancellation, resolves against current state and rechecks access before a fresh transaction. Tests cover move, split, inserted content, deletion, permission changes and session destruction.                                                                                                                                                                                                                          |
| Built workspace delivery                   | Fourteen library packages have explicit export maps, declarations and dependency-ordered builds. `apps/demo` is a Vite React workspace consuming supported exports. Built headless Node and React SSR plus vanilla/table/starter/custom-inline/React browser fixtures exercise public entry points; source-private imports and undeclared app imports fail ownership checks.                                                                                                   |
| Preserved demo and scale behavior          | E2E scenarios retain both routes, sample switching, outline navigation, comments, formatting, tables and selection. Production reflow covers 2,000/10,000 blocks and concurrent streaming in three browsers. Foundation benchmarks retain first paint, streaming, typing, paging, paste and heap budgets.                                                                                                                                                                      |
| Replaced interfaces removed                | No public engine factory or hybrid naming remains in setup. The manual React input/pointer/viewport hooks and their fixture are removed. Their behavior is verified through `EditorContent`; private view controllers remain implementation details. Supported guides use workspace package exports.                                                                                                                                                                           |

## Explicit limitations

The maintained [limitations inventory](editor-limitations.md) includes subsequent
fixes and the remaining collaboration, persistence, clipboard and validation gaps.

- The existing concurrent split/insert convergence test remains a TODO. This
  refactor does not implement a network collaboration transport or choose a new
  conflict-resolution algorithm.
- Text support is Latin, combining marks, common symbols and emoji. Complete
  international shaping and bidirectional editing remain outside this plan.
- A session supports one mounted view. React remains optional; React renderer
  registrations require a React content host.
- Markdown, JSON Schema generation, schema hot-swapping and a complete
  Tiptap-compatible extension catalog are outside this implementation wave.
- Packages remain private and unpublished. Consumers must serve the documented
  fonts/WASM assets or configure asset resolution.

The retired checklist extension is intentionally absent, as requested.
