import { defineContribution } from '@gprose/core';

/** Inert CSS installed and released with each mounted view, including in a shadow root. */
export const viewStyles = defineContribution<string>();
